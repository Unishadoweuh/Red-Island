ALTER TABLE `users` ADD `lk_points` INT(11) NOT NULL ;

-- Allez dans le SQL de votre serveur, allez dans "SQL" en haut, l'onglet, et entré la ligne au dessus, et faite exécuté. 
-- Cette ligne va permettre d'ajouté une ligne dans la table users sur chaque personne qui identifira la valeur du nombre de coins.